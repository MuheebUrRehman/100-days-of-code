function sandwich(item: string[]) {
  console.log(`making sandwich using ${item}`);
}

sandwich(["bread", "egg", "salt"]);
sandwich(["bread", "kabab", "ketchup"]);
sandwich(["bread", "mayo", "cucumber"]);
