type shape = {
  radius?: number;
  diameter?: number;
  length?: number;
  width?: number;
};

let circle: shape = {
  radius: 2,
  diameter: 4,
};

let rectangle: shape = {
  length: 8,
  width: 2,
};

console.log(circle);
console.log(rectangle);
